CREATE DATABASE problemsDB;
USE problemsDB;

CREATE TABLE problems (
    id INT AUTO_INCREMENT PRIMARY KEY,
    Name VARCHAR(255),
    Link VARCHAR(255),
    Type VARCHAR(100),
    TopicName VARCHAR(255)
);
ALTER TABLE problems ADD COLUMN Explanation VARCHAR(255) NULL, ADD COLUMN Code TEXT NULL, ADD COLUMN Video_link VARCHAR(255) NULL;

CREATE TABLE concepts (
  id INT AUTO_INCREMENT PRIMARY KEY,
  topic VARCHAR(255) NOT NULL,
  explanationEnglish TEXT,
  explanationBangla TEXT,
  code TEXT,
  input TEXT,
  output TEXT
);
ALTER TABLE concepts
ADD COLUMN subTopic VARCHAR(255);

CREATE TABLE resources (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    link TEXT NOT NULL,
    topic VARCHAR(255) NOT NULL
);


make a sql command for this . in sql command dont add id . add a attribute subTopic which will be "Condition" for every row. Dont change any variable name. table name is concepts

